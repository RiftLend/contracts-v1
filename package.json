{
  "name": "@riftlend/contracts-v1",
  "scripts": {
    "deploy:dev": "forge script scripts/deploy.s.sol --broadcast --private-key $DEPLOYER_PRIVATE_KEY --rpc-url http://127.0.0.1:9545 && forge script scripts/deploy.s.sol --broadcast --private-key $DEPLOYER_PRIVATE_KEY --rpc-url http://127.0.0.1:9546'",
    "install": "forge install",
    "build": "forge build",
    "test": "forge test",
    "init:env": "cp .env.example .env"
  },
  "dependencies": {
    "viem": "^2.21.37"
  }
}
